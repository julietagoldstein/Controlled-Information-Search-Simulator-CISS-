<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title>ChatGPT</title>

    <style>
        :root {
            --sidebar-width: 280px;
            --sidebar-bg: #f9f9f9;
            --main-bg: #ffffff;
            --hover-bg: #ececec;
            --active-bg: #ececec;
            --composer-bg: #ffffff;
            --user-bubble-bg: #f4f4f4;
            --border-color: #e5e5e5;
            --text-primary: #0d0d0d;
            --text-secondary: #676767;
            --text-muted: #8f8f8f;
            --content-width: 768px;
            --composer-width: 1000px;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        html,
        body {
            width: 100%;
            height: 100%;
        }

        body {
            display: flex;
            overflow: hidden;
            background: var(--main-bg);
            color: var(--text-primary);
            font-family:
                Inter,
                ui-sans-serif,
                -apple-system,
                BlinkMacSystemFont,
                "Segoe UI",
                Roboto,
                Helvetica,
                Arial,
                sans-serif;
            font-size: 16px;
        }

        button,
        textarea {
            font: inherit;
        }

        button {
            color: inherit;
        }

        button:focus-visible,
        textarea:focus-visible {
            outline: 2px solid #0d0d0d;
            outline-offset: 2px;
        }

        /* =========================
           BARRA LATERAL
        ========================= */

        #sidebar {
            width: var(--sidebar-width);
            flex-shrink: 0;
            display: flex;
            flex-direction: column;
            background: var(--sidebar-bg);
            border-right: 1px solid var(--border-color);
        }

        #sidebarTop {
            height: 60px;
            padding: 10px 14px;
            display: flex;
            align-items: center;
            justify-content: space-between;
        }

        #sidebarLogo {
            width: 30px;
            height: 30px;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        #sidebarLogo img {
            display: block;
            width: 27px;
            height: 27px;
            object-fit: contain;
        }

        #sidebarTopActions {
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .sidebar-icon-button {
            width: 30px;
            height: 30px;
            padding: 0;
            border: none;
            border-radius: 7px;
            display: flex;
            align-items: center;
            justify-content: center;
            background: transparent;
            color: #6f6f6f;
            cursor: default;
        }

        .sidebar-icon-button svg {
            width: 21px;
            height: 21px;
        }

        #sidebarNavigation {
            padding: 0 8px;
        }

        .sidebar-nav-button {
            width: 100%;
            min-height: 42px;
            padding: 8px 10px;
            border: none;
            border-radius: 10px;
            display: flex;
            align-items: center;
            gap: 11px;
            background: transparent;
            color: #171717;
            cursor: pointer;
            text-align: left;
            transition: background 0.15s ease;
        }

        .sidebar-nav-button:hover {
            background: var(--hover-bg);
        }

        .sidebar-nav-button.active {
            background: var(--active-bg);
        }

        .sidebar-nav-button svg {
            width: 21px;
            height: 21px;
            flex-shrink: 0;
        }

        .sidebar-nav-button span {
            font-size: 15px;
            line-height: 1.2;
        }

        #sidebarSpacer {
            flex: 1;
            min-height: 30px;
        }

        #sidebarUtilities {
            padding: 0 8px 13px;
        }

        .sidebar-utility {
            width: 100%;
            min-height: 42px;
            padding: 8px 10px;
            border-radius: 10px;
            display: flex;
            align-items: center;
            gap: 11px;
            color: #171717;
        }

        .sidebar-utility svg {
            width: 21px;
            height: 21px;
            flex-shrink: 0;
        }

        .sidebar-utility span {
            font-size: 15px;
        }

        #sidebarLoginPanel {
            padding: 22px 18px 18px;
            border-top: 1px solid #dddddd;
        }

        #sidebarLoginPanel h2 {
            margin-bottom: 16px;
            color: #111111;
            font-size: 15px;
            font-weight: 650;
            line-height: 1.35;
        }

        #sidebarLoginPanel p {
            margin-bottom: 22px;
            color: #929292;
            font-size: 14px;
            line-height: 1.55;
        }

        #sidebarLoginButton {
            width: 100%;
            min-height: 48px;
            border: 1px solid #d2d2d2;
            border-radius: 25px;
            background: #ffffff;
            color: #111111;
            font-size: 14px;
            font-weight: 650;
            cursor: default;
        }

        /* =========================
           CONTENIDO PRINCIPAL
        ========================= */

        #main {
            position: relative;
            flex: 1;
            min-width: 0;
            height: 100%;
            display: flex;
            flex-direction: column;
            background: var(--main-bg);
        }

        #header {
            min-height: 60px;
            flex-shrink: 0;
            padding: 0 24px 0 38px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            background: var(--main-bg);
        }

        #headerTitle {
            display: flex;
            align-items: center;
            gap: 5px;
            color: #111111;
            font-size: 20px;
            font-weight: 650;
            letter-spacing: -0.3px;
        }

        #headerTitle svg {
            width: 16px;
            height: 16px;
            color: #777777;
        }

        #headerActions {
            display: flex;
            align-items: center;
            gap: 9px;
        }

        .header-account-button {
            min-height: 40px;
            padding: 0 17px;
            border-radius: 22px;
            font-size: 14px;
            font-weight: 650;
            cursor: default;
            white-space: nowrap;
        }

        #loginButton {
            border: 1px solid #111111;
            background: #111111;
            color: #ffffff;
        }

        #signupButton {
            border: 1px solid #d5d5d5;
            background: #ffffff;
            color: #111111;
        }

        #mobileMenuButton {
            display: none;
            width: 40px;
            height: 40px;
            border: none;
            border-radius: 8px;
            background: transparent;
            align-items: center;
            justify-content: center;
        }

        #mobileMenuButton svg {
            width: 23px;
            height: 23px;
        }

        /* =========================
           PANTALLA INICIAL
        ========================= */

        #startScreen {
            position: absolute;
            z-index: 5;
            inset: 60px 0 0;
            padding: clamp(145px, 27vh, 255px) 24px 0;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: flex-start;
            background: #ffffff;
        }

        #welcome {
            width: 100%;
            max-width: var(--composer-width);
            margin-bottom: 30px;
            text-align: center;
        }

        #welcome h1 {
            color: #111111;
            font-size: 29px;
            font-weight: 400;
            line-height: 1.3;
            letter-spacing: -0.55px;
        }

        #welcome p {
            display: none;
        }

        body.chat-started #startScreen {
            display: none;
        }

        /* =========================
           ZONA DE MENSAJES
        ========================= */

        #messages {
            flex: 1;
            min-height: 0;
            padding: 30px 24px 150px;
            overflow-y: auto;
            overflow-anchor: none;
            scroll-behavior: smooth;
            scrollbar-width: thin;
            scrollbar-color: #c3c3c3 transparent;
        }

        #messages::-webkit-scrollbar {
            width: 8px;
        }

        #messages::-webkit-scrollbar-track {
            background: transparent;
        }

        #messages::-webkit-scrollbar-thumb {
            border-radius: 8px;
            background: #c3c3c3;
        }

        .message {
            width: 100%;
            max-width: var(--content-width);
            margin: 0 auto 32px;
            color: var(--text-primary);
            font-size: 16px;
            line-height: 1.65;
            scroll-margin-top: 18px;
        }

        .user-message {
            display: flex;
            justify-content: flex-end;
        }

        .user-bubble {
            width: fit-content;
            max-width: 75%;
            padding: 10px 16px;
            border-radius: 20px;
            background: var(--user-bubble-bg);
            color: #111111;
            line-height: 1.5;
            white-space: pre-wrap;
            overflow-wrap: anywhere;
        }

        .assistant-message {
            color: #171717;
            overflow-wrap: anywhere;
        }

        .assistant-text {
            white-space: pre-wrap;
        }

        .thinking {
            color: #676767;
        }

        .thinking .assistant-text {
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .thinking .assistant-text::before {
            content: "";
            width: 8px;
            height: 8px;
            flex-shrink: 0;
            border-radius: 50%;
            background: #676767;
            animation: thinkingPulse 1.1s ease-in-out infinite;
        }

        @keyframes thinkingPulse {
            0%,
            100% {
                opacity: 0.3;
                transform: scale(0.8);
            }

            50% {
                opacity: 1;
                transform: scale(1);
            }
        }

        /* =========================
           CONTENIDO DEL GUION
        ========================= */

        .assistant-content {
            color: #171717;
        }

        .assistant-content p {
            margin-bottom: 18px;
        }

        .assistant-content h2 {
            margin: 30px 0 14px;
            color: #111111;
            font-size: 21px;
            font-weight: 650;
            line-height: 1.35;
        }

        .assistant-content h2:first-child {
            margin-top: 0;
        }

        .assistant-content h3 {
            margin: 28px 0 13px;
            color: #111111;
            font-size: 18px;
            font-weight: 650;
            line-height: 1.45;
        }

        .assistant-content h3:first-child {
            margin-top: 0;
        }

        .assistant-content ol {
            margin: 0 0 18px 25px;
        }

        .assistant-content li {
            margin-bottom: 7px;
            padding-left: 4px;
        }

        .set-image {
            display: block;
            width: auto;
            max-width: 100%;
            max-height: 440px;
            margin: 20px 0 28px;
            border-radius: 12px;
            object-fit: contain;
        }

        /* =========================
           ÁREA DE ESCRITURA
        ========================= */

        #inputArea {
            width: 100%;
            max-width: var(--composer-width);
            flex-shrink: 0;
        }

        body.chat-started #inputArea {
            position: relative;
            z-index: 10;
            width: 100%;
            max-width: none;
            padding: 10px 24px 18px;
            background:
                linear-gradient(
                    to bottom,
                    rgba(255, 255, 255, 0),
                    #ffffff 24px
                );
        }

        #inputContainer {
            position: relative;
            width: 100%;
            max-width: var(--composer-width);
            min-height: 66px;
            margin: 0 auto;
            border: 1px solid #dddddd;
            border-radius: 34px;
            display: flex;
            align-items: center;
            background: var(--composer-bg);
            box-shadow:
                0 1px 2px rgba(0, 0, 0, 0.04),
                0 8px 25px rgba(0, 0, 0, 0.06);
        }

        #inputContainer:focus-within {
            border-color: #d2d2d2;
        }

        #addButton {
            width: 46px;
            height: 46px;
            margin-left: 8px;
            border: none;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            flex-shrink: 0;
            background: transparent;
            color: #171717;
            cursor: default;
        }

        #addButton svg {
            width: 25px;
            height: 25px;
        }

        #prompt {
            display: block;
            width: 100%;
            min-height: 56px;
            max-height: 180px;
            padding: 15px 145px 14px 4px;
            border: none;
            border-radius: 28px;
            outline: none;
            resize: none;
            overflow-y: hidden;
            background: transparent;
            color: #111111;
            font-size: 16px;
            line-height: 1.4;
        }

        #prompt::placeholder {
            color: #939393;
        }

        #composerActions {
            position: absolute;
            right: 9px;
            bottom: 8px;
            display: flex;
            align-items: center;
            gap: 7px;
        }

        #microphoneButton {
            width: 36px;
            height: 36px;
            border: none;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            background: transparent;
            color: #111111;
            cursor: default;
        }

        #microphoneButton svg {
            width: 21px;
            height: 21px;
        }

        #voiceButton {
            min-width: 82px;
            height: 46px;
            padding: 0 15px;
            border: none;
            border-radius: 24px;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
            background: #f0f0f0;
            color: #111111;
            font-size: 15px;
            font-weight: 650;
            cursor: default;
        }

        #voiceButton svg {
            width: 23px;
            height: 23px;
        }

        #sendButton {
            display: none;
            width: 40px;
            height: 40px;
            border: none;
            border-radius: 50%;
            align-items: center;
            justify-content: center;
            background: #111111;
            color: #ffffff;
            cursor: pointer;
            transition:
                opacity 0.15s ease,
                transform 0.1s ease;
        }

        #sendButton svg {
            width: 20px;
            height: 20px;
            stroke-width: 2.5;
        }

        #sendButton:active:not(:disabled) {
            transform: scale(0.94);
        }

        #sendButton:disabled {
            background: #d7d7d7;
            color: #ffffff;
            cursor: default;
        }

        #inputContainer.has-text #microphoneButton,
        #inputContainer.has-text #voiceButton {
            display: none;
        }

        #inputContainer.has-text #sendButton {
            display: flex;
        }

        #inputDisclaimer {
            display: none;
            max-width: var(--content-width);
            margin: 8px auto 0;
            color: #8d8d8d;
            font-size: 11px;
            line-height: 1.35;
            text-align: center;
        }

        body.chat-started #inputDisclaimer {
            display: block;
        }

        /* =========================
           RESPONSIVE
        ========================= */

        @media (max-width: 1050px) {
            :root {
                --sidebar-width: 250px;
            }

            #header {
                padding-left: 28px;
            }

            #sidebarLoginPanel p {
                font-size: 13px;
            }
        }

        @media (max-height: 720px) and (min-width: 761px) {
            #startScreen {
                padding-top: 115px;
            }

            #welcome {
                margin-bottom: 24px;
            }

            #sidebarLoginPanel {
                padding-top: 16px;
                padding-bottom: 14px;
            }

            #sidebarLoginPanel h2 {
                margin-bottom: 10px;
            }

            #sidebarLoginPanel p {
                margin-bottom: 15px;
                line-height: 1.4;
            }

            #sidebarLoginButton {
                min-height: 43px;
            }
        }

        @media (max-width: 760px) {
            #sidebar {
                display: none;
            }

            #header {
                min-height: 58px;
                padding: 0 12px;
                border-bottom: 1px solid var(--border-color);
            }

            #mobileMenuButton {
                display: flex;
            }

            #headerTitle {
                margin-right: auto;
                font-size: 19px;
            }

            #headerActions {
                gap: 7px;
            }

            .header-account-button {
                min-height: 39px;
                padding: 0 13px;
                font-size: 13px;
            }

            #startScreen {
                inset: 58px 0 0;
                padding: clamp(125px, 23vh, 190px) 12px 0;
            }

            #welcome {
                margin-bottom: 25px;
            }

            #welcome h1 {
                font-size: 26px;
            }

            #messages {
                padding: 24px 13px 130px;
            }

            body.chat-started #inputArea {
                padding: 9px 10px 13px;
            }

            #inputContainer {
                min-height: 62px;
                border-radius: 31px;
            }

            #addButton {
                width: 42px;
                height: 42px;
                margin-left: 7px;
            }

            #addButton svg {
                width: 24px;
                height: 24px;
            }

            #prompt {
                min-height: 54px;
                padding: 15px 112px 14px 2px;
                font-size: 16px;
            }

            #composerActions {
                right: 8px;
                bottom: 7px;
                gap: 4px;
            }

            #voiceButton {
                min-width: 71px;
                height: 45px;
                padding: 0 12px;
                font-size: 14px;
            }

            #voiceButton svg {
                width: 21px;
                height: 21px;
            }

            #microphoneButton {
                width: 36px;
                height: 36px;
            }

            .message {
                margin-bottom: 28px;
            }

            .user-bubble {
                max-width: 88%;
            }

            .set-image {
                max-height: 350px;
            }
        }

        @media (max-width: 480px) {
            #signupButton {
                display: none;
            }

            #headerTitle {
                font-size: 18px;
            }

            #startScreen {
                padding-top: 120px;
            }

            #welcome h1 {
                font-size: 24px;
            }

            .message {
                font-size: 15px;
            }

            .assistant-content h2 {
                font-size: 20px;
            }

            .assistant-content h3 {
                font-size: 17px;
            }

            #voiceButton span {
                display: none;
            }

            #voiceButton {
                min-width: 45px;
                width: 45px;
                padding: 0;
            }

            #prompt {
                padding-right: 93px;
            }

            #inputDisclaimer {
                display: none !important;
            }
        }
    </style>
</head>

<body>

    <aside id="sidebar">

        <div id="sidebarTop">

            <div id="sidebarLogo">
                <img
                    src="Images/ChatGPT/chatgpt-logo.png"
                    alt="ChatGPT"
                >
            </div>

            <div id="sidebarTopActions">

                <button
                    class="sidebar-icon-button"
                    type="button"
                    aria-label="Buscar"
                    title="Buscar"
                >
                    <svg
                        viewBox="0 0 24 24"
                        fill="none"
                        xmlns="http://www.w3.org/2000/svg"
                        aria-hidden="true"
                    >
                        <circle
                            cx="11"
                            cy="11"
                            r="7"
                            stroke="currentColor"
                            stroke-width="1.8"
                        />
                        <path
                            d="m16.5 16.5 4 4"
                            stroke="currentColor"
                            stroke-width="1.8"
                            stroke-linecap="round"
                        />
                    </svg>
                </button>

                <button
                    class="sidebar-icon-button"
                    type="button"
                    aria-label="Ocultar barra lateral"
                    title="Ocultar barra lateral"
                >
                    <svg
                        viewBox="0 0 24 24"
                        fill="none"
                        xmlns="http://www.w3.org/2000/svg"
                        aria-hidden="true"
                    >
                        <rect
                            x="3"
                            y="4"
                            width="18"
                            height="16"
                            rx="4"
                            stroke="currentColor"
                            stroke-width="1.7"
                        />
                        <path
                            d="M9 4v16"
                            stroke="currentColor"
                            stroke-width="1.7"
                        />
                    </svg>
                </button>

            </div>

        </div>

        <nav id="sidebarNavigation">

            <button
                id="newChatButton"
                class="sidebar-nav-button active"
                type="button"
            >
                <svg
                    viewBox="0 0 24 24"
                    fill="none"
                    xmlns="http://www.w3.org/2000/svg"
                    aria-hidden="true"
                >
                    <path
                        d="M14 5H6.8A2.8 2.8 0 0 0 4 7.8v9.4A2.8 2.8 0 0 0 6.8 20h9.4a2.8 2.8 0 0 0 2.8-2.8V10"
                        stroke="currentColor"
                        stroke-width="1.7"
                        stroke-linecap="round"
                        stroke-linejoin="round"
                    />
                    <path
                        d="m13 11 7-7"
                        stroke="currentColor"
                        stroke-width="1.7"
                        stroke-linecap="round"
                    />
                    <path
                        d="M16 4h4v4"
                        stroke="currentColor"
                        stroke-width="1.7"
                        stroke-linecap="round"
                        stroke-linejoin="round"
                    />
                </svg>

                <span>Nuevo chat</span>
            </button>

            <button
                class="sidebar-nav-button"
                type="button"
                tabindex="-1"
            >
                <svg
                    viewBox="0 0 24 24"
                    fill="none"
                    xmlns="http://www.w3.org/2000/svg"
                    aria-hidden="true"
                >
                    <rect
                        x="4"
                        y="5"
                        width="14"
                        height="14"
                        rx="3"
                        stroke="currentColor"
                        stroke-width="1.7"
                    />
                    <path
                        d="m6.5 16 3.2-3.5 2.5 2.5 1.9-2 3.4 3"
                        stroke="currentColor"
                        stroke-width="1.7"
                        stroke-linecap="round"
                        stroke-linejoin="round"
                    />
                    <circle
                        cx="14.5"
                        cy="9"
                        r="1.2"
                        fill="currentColor"
                    />
                    <path
                        d="M18 3.8h1.2A1.8 1.8 0 0 1 21 5.6v7.2"
                        stroke="currentColor"
                        stroke-width="1.7"
                        stroke-linecap="round"
                    />
                </svg>

                <span>Imágenes</span>
            </button>

        </nav>

        <div id="sidebarSpacer"></div>

        <div id="sidebarUtilities">

            <div class="sidebar-utility">
                <svg
                    viewBox="0 0 24 24"
                    fill="none"
                    xmlns="http://www.w3.org/2000/svg"
                    aria-hidden="true"
                >
                    <path
                        d="m12 3 8 4.5v9L12 21l-8-4.5v-9L12 3Z"
                        stroke="currentColor"
                        stroke-width="1.7"
                        stroke-linejoin="round"
                    />
                    <path
                        d="M12 8v8M8 12h8"
                        stroke="currentColor"
                        stroke-width="1.7"
                        stroke-linecap="round"
                    />
                </svg>

                <span>Ver planes y precios</span>
            </div>

            <div class="sidebar-utility">
                <svg
                    viewBox="0 0 24 24"
                    fill="none"
                    xmlns="http://www.w3.org/2000/svg"
                    aria-hidden="true"
                >
                    <path
                        d="M9.5 3.7 10 2h4l.5 1.7 1.6.7 1.6-.8 2.8 2.8-.8 1.6.7 1.6 1.6.5v4l-1.6.5-.7 1.6.8 1.6-2.8 2.8-1.6-.8-1.6.7L14 22h-4l-.5-1.7-1.6-.7-1.6.8-2.8-2.8.8-1.6-.7-1.6L2 14v-4l1.6-.5.7-1.6-.8-1.6 2.8-2.8 1.6.8 1.6-.6Z"
                        stroke="currentColor"
                        stroke-width="1.4"
                        stroke-linejoin="round"
                    />
                    <circle
                        cx="12"
                        cy="12"
                        r="3"
                        stroke="currentColor"
                        stroke-width="1.6"
                    />
                </svg>

                <span>Configuración</span>
            </div>

            <div class="sidebar-utility">
                <svg
                    viewBox="0 0 24 24"
                    fill="none"
                    xmlns="http://www.w3.org/2000/svg"
                    aria-hidden="true"
                >
                    <circle
                        cx="12"
                        cy="12"
                        r="9"
                        stroke="currentColor"
                        stroke-width="1.7"
                    />
                    <circle
                        cx="12"
                        cy="12"
                        r="4"
                        stroke="currentColor"
                        stroke-width="1.7"
                    />
                    <path
                        d="m5.7 5.7 3.5 3.5M14.8 14.8l3.5 3.5M18.3 5.7l-3.5 3.5M9.2 14.8l-3.5 3.5"
                        stroke="currentColor"
                        stroke-width="1.7"
                        stroke-linecap="round"
                    />
                </svg>

                <span>Ayuda</span>
            </div>

        </div>

        <div id="sidebarLoginPanel">
            <h2>Obtén respuestas adaptadas a ti</h2>

            <p>
                Inicia sesión para obtener respuestas basadas en chats
                guardados, además de crear imágenes y cargar archivos.
            </p>

            <button id="sidebarLoginButton" type="button">
                Iniciar sesión
            </button>
        </div>

    </aside>

    <main id="main">

        <header id="header">

            <button
                id="mobileMenuButton"
                type="button"
                aria-label="Abrir menú"
            >
                <svg
                    viewBox="0 0 24 24"
                    fill="none"
                    xmlns="http://www.w3.org/2000/svg"
                    aria-hidden="true"
                >
                    <path
                        d="M4 7h16M4 12h16M4 17h16"
                        stroke="currentColor"
                        stroke-width="1.8"
                        stroke-linecap="round"
                    />
                </svg>
            </button>

            <div id="headerTitle">
                <span>ChatGPT</span>

                <svg
                    viewBox="0 0 24 24"
                    fill="none"
                    xmlns="http://www.w3.org/2000/svg"
                    aria-hidden="true"
                >
                    <path
                        d="m8 10 4 4 4-4"
                        stroke="currentColor"
                        stroke-width="2"
                        stroke-linecap="round"
                        stroke-linejoin="round"
                    />
                </svg>
            </div>

            <div id="headerActions">
                <button
                    id="loginButton"
                    class="header-account-button"
                    type="button"
                >
                    Iniciar sesión
                </button>

                <button
                    id="signupButton"
                    class="header-account-button"
                    type="button"
                >
                    Registrarse gratis
                </button>
            </div>

        </header>

        <div id="startScreen">

            <div id="welcome">
                <h1>¿Qué toca hoy?</h1>
                <p>Pregunta lo que quieras</p>
            </div>

            <div id="inputArea">

                <div id="inputContainer">

                    <button
                        id="addButton"
                        type="button"
                        aria-label="Añadir"
                        title="Añadir"
                    >
                        <svg
                            viewBox="0 0 24 24"
                            fill="none"
                            xmlns="http://www.w3.org/2000/svg"
                            aria-hidden="true"
                        >
                            <path
                                d="M12 5v14M5 12h14"
                                stroke="currentColor"
                                stroke-width="1.8"
                                stroke-linecap="round"
                            />
                        </svg>
                    </button>

                    <textarea
                        id="prompt"
                        rows="1"
                        placeholder="Pregunta lo que quieras"
                        aria-label="Mensaje"
                    ></textarea>

                    <div id="composerActions">

                        <button
                            id="microphoneButton"
                            type="button"
                            aria-label="Micrófono"
                            title="Micrófono"
                        >
                            <svg
                                viewBox="0 0 24 24"
                                fill="none"
                                xmlns="http://www.w3.org/2000/svg"
                                aria-hidden="true"
                            >
                                <rect
                                    x="9"
                                    y="3"
                                    width="6"
                                    height="12"
                                    rx="3"
                                    stroke="currentColor"
                                    stroke-width="1.8"
                                />
                                <path
                                    d="M5.5 11.5a6.5 6.5 0 0 0 13 0M12 18v3M9 21h6"
                                    stroke="currentColor"
                                    stroke-width="1.8"
                                    stroke-linecap="round"
                                />
                            </svg>
                        </button>

                        <button
                            id="voiceButton"
                            type="button"
                            aria-label="Voz"
                        >
                            <svg
                                viewBox="0 0 24 24"
                                fill="none"
                                xmlns="http://www.w3.org/2000/svg"
                                aria-hidden="true"
                            >
                                <path
                                    d="M4 10v4M8 7v10M12 4v16M16 7v10M20 10v4"
                                    stroke="currentColor"
                                    stroke-width="1.9"
                                    stroke-linecap="round"
                                />
                            </svg>

                            <span>Voz</span>
                        </button>

                        <button
                            id="sendButton"
                            type="button"
                            disabled
                            aria-label="Enviar mensaje"
                            title="Enviar mensaje"
                        >
                            <svg
                                viewBox="0 0 24 24"
                                fill="none"
                                xmlns="http://www.w3.org/2000/svg"
                                aria-hidden="true"
                            >
                                <path
                                    d="M12 19V5"
                                    stroke="currentColor"
                                    stroke-linecap="round"
                                />
                                <path
                                    d="m6.5 10.5 5.5-5.5 5.5 5.5"
                                    stroke="currentColor"
                                    stroke-linecap="round"
                                    stroke-linejoin="round"
                                />
                            </svg>
                        </button>

                    </div>

                </div>

                <div id="inputDisclaimer">
                    ChatGPT puede cometer errores. Considera verificar la información importante.
                </div>

            </div>

        </div>

        <div id="messages"></div>

    </main>

    <script>
        const main = document.getElementById("main");
        const startScreen = document.getElementById("startScreen");
        const inputArea = document.getElementById("inputArea");
        const promptInput = document.getElementById("prompt");
        const sendButton = document.getElementById("sendButton");
        const messages = document.getElementById("messages");
        const newChatButton = document.getElementById("newChatButton");
        const inputContainer = document.getElementById("inputContainer");

        const Category = Object.freeze({
            GREETING: "GREETING",
            GENERAL: "GENERAL",
            RULE: "RULE",
            EXAMPLE_1: "EXAMPLE_1",
            EXAMPLE_2: "EXAMPLE_2",
            EXAMPLE_3: "EXAMPLE_3",
            EXAMPLE_4: "EXAMPLE_4",
            OFF_TOPIC: "OFF_TOPIC"
        });

        const GREETING_RESPONSE =
            "¡Hola! 😊 ¿Cómo te puedo ayudar?";

        const OFF_TOPIC_RESPONSE =
            "Ahora estamos aprendiendo a jugar a SET. Hazme una pregunta sobre el juego y estaré encantado de ayudarte.";

        const TOKEN_GROUPS = Object.freeze({
            SET_CONTEXT: [
                "set",
                "cartas",
                "carta",
                "juego"
            ],

            GENERAL: [
                "jugar",
                "juega",
                "jugamos",
                "aprender",
                "aprendo",
                "ensenar",
                "ensename",
                "ensenarme",
                "explicar",
                "explicame",
                "instrucciones",
                "empezar",
                "comenzar"
            ],

            RULE: [
                "regla",
                "reglas",
                "formar",
                "forman",
                "forma",
                "cumplir",
                "cumple",
                "caracteristica",
                "caracteristicas",
                "iguales",
                "distintas",
                "diferentes",
                "saber",
                "descubrir"
            ],

            EXAMPLE: [
                "ejemplo",
                "ejemplos",
                "mostrar",
                "mostrame",
                "muestrame",
                "explicar",
                "explicame",
                "entender"
            ],

            EXAMPLE_1: [
                "1",
                "uno",
                "primer",
                "primero",
                "iguales"
            ],

            EXAMPLE_2: [
                "2",
                "dos",
                "segundo",
                "segunda",
                "distintas",
                "diferentes"
            ],

            EXAMPLE_3: [
                "3",
                "tres",
                "tercer",
                "tercero",
                "mezcla",
                "combinacion"
            ],

            EXAMPLE_4: [
                "4",
                "cuatro",
                "cuarto",
                "rompe",
                "romper",
                "verdes",
                "verde",
                "azul"
            ]
        });

        const STOP_WORDS = new Set([
            "a",
            "al",
            "algo",
            "como",
            "con",
            "cual",
            "cuales",
            "de",
            "del",
            "el",
            "en",
            "es",
            "esa",
            "ese",
            "esta",
            "este",
            "esto",
            "hay",
            "la",
            "las",
            "lo",
            "los",
            "me",
            "mi",
            "no",
            "o",
            "para",
            "por",
            "porque",
            "que",
            "se",
            "si",
            "son",
            "su",
            "te",
            "tiene",
            "un",
            "una",
            "unas",
            "unos",
            "y",
            "yo",
            "quiero",
            "puedo",
            "podes",
            "podrias"
        ]);

        const fullSetScript = `
            <div class="assistant-content">
                <h2>Instrucciones para Jugar al SET</h2>

                <p>En el juego hay cartas, y estas cartas tienen dibujitos.</p>

                <img
                    class="set-image"
                    src="Images/intro-set.png"
                    alt="Cartas del juego SET"
                >

                <p>Entonces, tenemos que descubrir si las cartas forman un SET, o no.</p>

                <h2>Regla para formar un SET</h2>

                <p>Para formar un “set”, cada característica (forma, color, relleno, y cantidad) tiene que cumplir una regla.</p>

                <p>Esa regla dice que tienen que ser: o todas iguales, o todas distintas en esa característica.</p>

                <p>Si las cuatro características cumplen la regla, entonces las cartas forman un SET.</p>

                <img
                    class="set-image"
                    src="Images/set.png"
                    alt="Ejemplo de cartas que forman un SET"
                >

                <p>Pero, si alguna característica no cumple la regla: o sea, si en alguna característica no son ni todas iguales ni todas distintas, entonces las cartas ya NO forman un SET.</p>

                <img
                    class="set-image"
                    src="Images/noSet.png"
                    alt="Ejemplo de cartas que no forman un SET"
                >

                <h2>Veamos algunos ejemplos</h2>

                <h3>1) Todas iguales → Sí forman un SET</h3>

                <img
                    class="set-image"
                    src="Images/ejemplo1.png"
                    alt="Ejemplo de cartas con características iguales"
                >

                <p>¿Se cumple la regla “todas iguales o todas distintas”?</p>

                <ol>
                    <li>Todas tienen la misma forma.</li>
                    <li>Todas tienen el mismo color.</li>
                    <li>Todas tienen el mismo relleno.</li>
                    <li>Todas tienen la misma cantidad.</li>
                </ol>

                <p>Entonces, estas cartas forman un SET.</p>

                <h3>2) Todas distintas → Sí forman un SET</h3>

                <img
                    class="set-image"
                    src="Images/ejemplo2.png"
                    alt="Ejemplo de cartas con características distintas"
                >

                <ol>
                    <li>Las formas son todas distintas.</li>
                    <li>Los colores son todos distintos.</li>
                    <li>Los rellenos son todos distintos.</li>
                    <li>Las cantidades son todas distintas.</li>
                </ol>

                <p>Como todas cumplen la regla, forman un SET.</p>

                <h3>3) Mezcla de iguales y distintas → Sí forman un SET</h3>

                <img
                    class="set-image"
                    src="Images/ejemplo3.png"
                    alt="Ejemplo con características iguales y distintas"
                >

                <ol>
                    <li>Las formas son iguales.</li>
                    <li>Los colores son distintos.</li>
                    <li>Los rellenos son iguales.</li>
                    <li>Las cantidades son distintas.</li>
                </ol>

                <p>La regla se cumple en todas las características.</p>

                <h3>4) Una característica rompe la regla → NO forman un SET</h3>

                <img
                    class="set-image"
                    src="Images/ejemplo4.png"
                    alt="Ejemplo en el que una característica rompe la regla"
                >

                <ol>
                    <li>Las formas sí cumplen la regla.</li>
                    <li>El color NO cumple la regla: Hay dos verdes iguales y una azul distinta.</li>
                </ol>

                <p>Entonces, estas cartas NO forman un SET.</p>
            </div>
        `;

        const ruleScript = `
            <div class="assistant-content">
                <h2>Regla para formar un SET</h2>

                <p>Para formar un “set”, cada característica (forma, color, relleno, y cantidad) tiene que cumplir una regla.</p>

                <p>Esa regla dice que tienen que ser: o todas iguales, o todas distintas en esa característica.</p>

                <p>Si las cuatro características cumplen la regla, entonces las cartas forman un SET.</p>

                <img
                    class="set-image"
                    src="Images/set.png"
                    alt="Ejemplo de cartas que forman un SET"
                >

                <p>Pero, si alguna característica no cumple la regla: o sea, si en alguna característica no son ni todas iguales ni todas distintas, entonces las cartas ya NO forman un SET.</p>

                <img
                    class="set-image"
                    src="Images/noSet.png"
                    alt="Ejemplo de cartas que no forman un SET"
                >
            </div>
        `;

        const example1Script = `
            <div class="assistant-content">
                <h3>1) Todas iguales → Sí forman un SET</h3>

                <img
                    class="set-image"
                    src="Images/ejemplo1.png"
                    alt="Ejemplo de cartas con características iguales"
                >

                <p>¿Se cumple la regla “todas iguales o todas distintas”?</p>

                <ol>
                    <li>Todas tienen la misma forma.</li>
                    <li>Todas tienen el mismo color.</li>
                    <li>Todas tienen el mismo relleno.</li>
                    <li>Todas tienen la misma cantidad.</li>
                </ol>

                <p>Entonces, estas cartas forman un SET.</p>
            </div>
        `;

        const example2Script = `
            <div class="assistant-content">
                <h3>2) Todas distintas → Sí forman un SET</h3>

                <img
                    class="set-image"
                    src="Images/ejemplo2.png"
                    alt="Ejemplo de cartas con características distintas"
                >

                <ol>
                    <li>Las formas son todas distintas.</li>
                    <li>Los colores son todos distintos.</li>
                    <li>Los rellenos son todos distintos.</li>
                    <li>Las cantidades son todas distintas.</li>
                </ol>

                <p>Como todas cumplen la regla, forman un SET.</p>
            </div>
        `;

        const example3Script = `
            <div class="assistant-content">
                <h3>3) Mezcla de iguales y distintas → Sí forman un SET</h3>

                <img
                    class="set-image"
                    src="Images/ejemplo3.png"
                    alt="Ejemplo con características iguales y distintas"
                >

                <ol>
                    <li>Las formas son iguales.</li>
                    <li>Los colores son distintos.</li>
                    <li>Los rellenos son iguales.</li>
                    <li>Las cantidades son distintas.</li>
                </ol>

                <p>La regla se cumple en todas las características.</p>
            </div>
        `;

        const example4Script = `
            <div class="assistant-content">
                <h3>4) Una característica rompe la regla → NO forman un SET</h3>

                <img
                    class="set-image"
                    src="Images/ejemplo4.png"
                    alt="Ejemplo en el que una característica rompe la regla"
                >

                <ol>
                    <li>Las formas sí cumplen la regla.</li>
                    <li>El color NO cumple la regla: Hay dos verdes iguales y una azul distinta.</li>
                </ol>

                <p>Entonces, estas cartas NO forman un SET.</p>
            </div>
        `;

        function normalizeText(text) {
            return text
                .toLowerCase()
                .normalize("NFD")
                .replace(/[\u0300-\u036f]/g, "")
                .replace(/[^a-z0-9ñ\s]/g, " ")
                .replace(/\s+/g, " ")
                .trim();
        }

        function isGreeting(text) {
            const normalized = normalizeText(text);

            const greetingPatterns = [
                /^hola+$/,
                /^hola+ como estas$/,
                /^hola+ que tal$/,
                /^ola+$/,
                /^buenas$/,
                /^buen dia$/,
                /^buenos dias$/,
                /^buenas tardes$/,
                /^buenas noches$/,
                /^hey$/
            ];

            return greetingPatterns.some(pattern =>
                pattern.test(normalized)
            );
        }

        function tokenize(text) {
            const normalized = normalizeText(text);

            if (!normalized) {
                return [];
            }

            return normalized
                .split(" ")
                .filter(token =>
                    token && !STOP_WORDS.has(token)
                );
        }

        function levenshteinDistance(firstWord, secondWord) {
            const first = String(firstWord);
            const second = String(secondWord);

            const rows = first.length + 1;
            const columns = second.length + 1;

            const matrix = Array.from(
                { length: rows },
                () => Array(columns).fill(0)
            );

            for (let row = 0; row < rows; row += 1) {
                matrix[row][0] = row;
            }

            for (let column = 0; column < columns; column += 1) {
                matrix[0][column] = column;
            }

            for (let row = 1; row < rows; row += 1) {
                for (let column = 1; column < columns; column += 1) {
                    const substitutionCost =
                        first[row - 1] === second[column - 1]
                            ? 0
                            : 1;

                    matrix[row][column] = Math.min(
                        matrix[row - 1][column] + 1,
                        matrix[row][column - 1] + 1,
                        matrix[row - 1][column - 1] +
                            substitutionCost
                    );
                }
            }

            return matrix[rows - 1][columns - 1];
        }

        function getAllowedDistance(targetWord) {
            const length = targetWord.length;

            if (length <= 3) {
                return 0;
            }

            if (length <= 6) {
                return 1;
            }

            return 2;
        }

        function isKnownShortVariant(
            inputToken,
            targetToken
        ) {
            if (targetToken === "set") {
                return [
                    "set",
                    "zet"
                ].includes(inputToken);
            }

            return false;
        }

        function tokensAreSimilar(
            inputToken,
            targetToken
        ) {
            if (inputToken === targetToken) {
                return true;
            }

            if (
                isKnownShortVariant(
                    inputToken,
                    targetToken
                )
            ) {
                return true;
            }

            const allowedDistance =
                getAllowedDistance(targetToken);

            if (allowedDistance === 0) {
                return false;
            }

            const lengthDifference = Math.abs(
                inputToken.length -
                targetToken.length
            );

            if (
                lengthDifference >
                allowedDistance
            ) {
                return false;
            }

            return levenshteinDistance(
                inputToken,
                targetToken
            ) <= allowedDistance;
        }

        function tokenListContains(
            tokens,
            targetWord
        ) {
            return tokens.some(token =>
                tokensAreSimilar(
                    token,
                    targetWord
                )
            );
        }

        function tokenListContainsAny(
            tokens,
            targetWords
        ) {
            return targetWords.some(targetWord =>
                tokenListContains(
                    tokens,
                    targetWord
                )
            );
        }

        function countMatches(
            tokens,
            targetWords
        ) {
            const matchedInputTokens =
                new Set();

            targetWords.forEach(targetWord => {
                const matchingToken =
                    tokens.find(token =>
                        !matchedInputTokens.has(token) &&
                        tokensAreSimilar(
                            token,
                            targetWord
                        )
                    );

                if (matchingToken) {
                    matchedInputTokens.add(
                        matchingToken
                    );
                }
            });

            return matchedInputTokens.size;
        }

        function hasExampleMarker(tokens) {
            return tokenListContainsAny(
                tokens,
                TOKEN_GROUPS.EXAMPLE
            );
        }

        function hasExactNumber(
            tokens,
            number,
            word
        ) {
            return tokens.includes(number) ||
                tokenListContains(tokens, word);
        }

        function classifySpecificExample(tokens) {
            const exampleMarker =
                hasExampleMarker(tokens);

            const hasOne =
                hasExactNumber(
                    tokens,
                    "1",
                    "uno"
                ) ||
                tokenListContainsAny(
                    tokens,
                    ["primer", "primero"]
                );

            const hasTwo =
                hasExactNumber(
                    tokens,
                    "2",
                    "dos"
                ) ||
                tokenListContainsAny(
                    tokens,
                    ["segundo", "segunda"]
                );

            const hasThree =
                hasExactNumber(
                    tokens,
                    "3",
                    "tres"
                ) ||
                tokenListContainsAny(
                    tokens,
                    ["tercer", "tercero"]
                );

            const hasFour =
                hasExactNumber(
                    tokens,
                    "4",
                    "cuatro"
                ) ||
                tokenListContains(
                    tokens,
                    "cuarto"
                );

            const hasGreenAndBlue =
                tokenListContainsAny(
                    tokens,
                    ["verde", "verdes"]
                ) &&
                tokenListContains(
                    tokens,
                    "azul"
                );

            const hasBrokenRule =
                tokenListContainsAny(
                    tokens,
                    ["rompe", "romper"]
                );

            const hasMixture =
                tokenListContainsAny(
                    tokens,
                    ["mezcla", "combinacion"]
                );

            if (
                (exampleMarker && hasFour) ||
                hasGreenAndBlue ||
                hasBrokenRule
            ) {
                return Category.EXAMPLE_4;
            }

            if (
                (exampleMarker && hasThree) ||
                hasMixture
            ) {
                return Category.EXAMPLE_3;
            }

            if (
                exampleMarker &&
                hasTwo
            ) {
                return Category.EXAMPLE_2;
            }

            if (
                exampleMarker &&
                hasOne
            ) {
                return Category.EXAMPLE_1;
            }

            return null;
        }

        function calculateCategoryScores(tokens) {
            const scores = {
                [Category.GENERAL]: 0,
                [Category.RULE]: 0,
                [Category.EXAMPLE_1]: 0,
                [Category.EXAMPLE_2]: 0,
                [Category.EXAMPLE_3]: 0,
                [Category.EXAMPLE_4]: 0
            };

            const setContextScore =
                countMatches(
                    tokens,
                    TOKEN_GROUPS.SET_CONTEXT
                );

            scores[Category.GENERAL] +=
                countMatches(
                    tokens,
                    TOKEN_GROUPS.GENERAL
                ) * 2;

            scores[Category.RULE] +=
                countMatches(
                    tokens,
                    TOKEN_GROUPS.RULE
                ) * 2;

            scores[Category.EXAMPLE_1] +=
                countMatches(
                    tokens,
                    TOKEN_GROUPS.EXAMPLE_1
                );

            scores[Category.EXAMPLE_2] +=
                countMatches(
                    tokens,
                    TOKEN_GROUPS.EXAMPLE_2
                );

            scores[Category.EXAMPLE_3] +=
                countMatches(
                    tokens,
                    TOKEN_GROUPS.EXAMPLE_3
                ) * 2;

            scores[Category.EXAMPLE_4] +=
                countMatches(
                    tokens,
                    TOKEN_GROUPS.EXAMPLE_4
                ) * 2;

            if (setContextScore > 0) {
                scores[Category.GENERAL] += 1;
                scores[Category.RULE] += 1;
            }

            return scores;
        }

        function getHighestScoringCategory(scores) {
            const categoryPriority = [
                Category.EXAMPLE_4,
                Category.EXAMPLE_3,
                Category.EXAMPLE_2,
                Category.EXAMPLE_1,
                Category.RULE,
                Category.GENERAL
            ];

            let bestCategory =
                Category.OFF_TOPIC;

            let bestScore = 0;

            categoryPriority.forEach(category => {
                const score = scores[category];

                if (score > bestScore) {
                    bestCategory = category;
                    bestScore = score;
                }
            });

            return {
                category: bestCategory,
                score: bestScore
            };
        }

        function classifyQuestion(text) {
            if (isGreeting(text)) {
                return Category.GREETING;
            }

            const tokens = tokenize(text);

            if (tokens.length === 0) {
                return Category.OFF_TOPIC;
            }

            const specificExample =
                classifySpecificExample(tokens);

            if (specificExample) {
                return specificExample;
            }

            const scores =
                calculateCategoryScores(tokens);

            const result =
                getHighestScoringCategory(scores);

            const hasRuleContrast =
                tokenListContains(
                    tokens,
                    "iguales"
                ) ||
                tokenListContainsAny(
                    tokens,
                    [
                        "distintas",
                        "diferentes"
                    ]
                );

            if (
                hasRuleContrast &&
                !hasExampleMarker(tokens)
            ) {
                return Category.RULE;
            }

            if (result.score < 2) {
                return Category.OFF_TOPIC;
            }

            return result.category;
        }

        function updateButton() {
            const hasText =
                promptInput.value.trim() !== "";

            sendButton.disabled = !hasText;

            inputContainer.classList.toggle(
                "has-text",
                hasText
            );
        }

        function resizeTextarea() {
            promptInput.style.height = "auto";

            const newHeight = Math.min(
                promptInput.scrollHeight,
                180
            );

            promptInput.style.height =
                newHeight + "px";

            promptInput.style.overflowY =
                promptInput.scrollHeight > 180
                    ? "auto"
                    : "hidden";
        }

        function addUserMessage(text) {
            const wrapper =
                document.createElement("div");

            wrapper.className =
                "message user-message";

            const bubble =
                document.createElement("div");

            bubble.className =
                "user-bubble";

            bubble.textContent = text;

            wrapper.appendChild(bubble);
            messages.appendChild(wrapper);

            return wrapper;
        }

        function addAssistantMessage(
            text,
            extraClass = ""
        ) {
            const message =
                document.createElement("div");

            message.className =
                `message assistant-message ${extraClass}`.trim();

            const content =
                document.createElement("div");

            content.className =
                "assistant-text";

            content.textContent = text;

            message.appendChild(content);
            messages.appendChild(message);

            return message;
        }

        function addAssistantHTML(html) {
            const message =
                document.createElement("div");

            message.className =
                "message assistant-message";

            message.innerHTML = html;
            messages.appendChild(message);

            return message;
        }

        function positionAtUserMessage(userMessage) {
            requestAnimationFrame(() => {
                userMessage.scrollIntoView({
                    behavior: "smooth",
                    block: "start"
                });
            });
        }

        function getAssistantResponse(category) {
            switch (category) {
                case Category.GREETING:
                    return {
                        type: "text",
                        content: GREETING_RESPONSE
                    };

                case Category.GENERAL:
                    return {
                        type: "html",
                        content: fullSetScript
                    };

                case Category.RULE:
                    return {
                        type: "html",
                        content: ruleScript
                    };

                case Category.EXAMPLE_1:
                    return {
                        type: "html",
                        content: example1Script
                    };

                case Category.EXAMPLE_2:
                    return {
                        type: "html",
                        content: example2Script
                    };

                case Category.EXAMPLE_3:
                    return {
                        type: "html",
                        content: example3Script
                    };

                case Category.EXAMPLE_4:
                    return {
                        type: "html",
                        content: example4Script
                    };

                case Category.OFF_TOPIC:
                default:
                    return {
                        type: "text",
                        content: OFF_TOPIC_RESPONSE
                    };
            }
        }

        function displayAssistantResponse(response) {
            if (response.type === "html") {
                return addAssistantHTML(
                    response.content
                );
            }

            return addAssistantMessage(
                response.content
            );
        }

        function moveInputToChat() {
            main.appendChild(inputArea);
        }

        function moveInputToStartScreen() {
            startScreen.appendChild(inputArea);
        }

        function sendMessage() {
            const text =
                promptInput.value.trim();

            if (!text) {
                return;
            }

            document.body.classList.add(
                "chat-started"
            );

            moveInputToChat();

            const userMessage =
                addUserMessage(text);

            promptInput.value = "";
            resizeTextarea();
            updateButton();

            const thinkingMessage =
                addAssistantMessage(
                    "Pensando",
                    "thinking"
                );

            positionAtUserMessage(userMessage);

            window.setTimeout(() => {
                const category =
                    classifyQuestion(text);

                const response =
                    getAssistantResponse(category);

                thinkingMessage.remove();
                displayAssistantResponse(response);
            }, 800);
        }

        function resetChat() {
            const existingMessages =
                messages.querySelectorAll(
                    ".message"
                );

            existingMessages.forEach(message => {
                message.remove();
            });

            document.body.classList.remove(
                "chat-started"
            );

            moveInputToStartScreen();

            promptInput.value = "";
            resizeTextarea();
            updateButton();

            messages.scrollTop = 0;
            promptInput.focus();
        }

        promptInput.addEventListener(
            "input",
            () => {
                updateButton();
                resizeTextarea();
            }
        );

        promptInput.addEventListener(
            "keydown",
            event => {
                if (
                    event.key === "Enter" &&
                    !event.shiftKey &&
                    !event.isComposing
                ) {
                    event.preventDefault();
                    sendMessage();
                }
            }
        );

        sendButton.addEventListener(
            "click",
            sendMessage
        );

        newChatButton.addEventListener(
            "click",
            resetChat
        );

        resizeTextarea();
        updateButton();
    </script>

</body>
</html>
